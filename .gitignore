node_modules/
dist/
dist-server/
build/
.env
.env.local
*.log

# Source WordPress backup (1.7GB) and everything extracted from it.
backup_*.tar
migrate/data/
migrate/uploads/
.superpowers/

# docker-compose.dev.yml volume mounts
.dev/
.gstack/

# Copies of the artist's texts pulled off the old WordPress host before it
# is decommissioned (see docs/development.md). Binary .doc/.pdf originals --
# not site content, and not ours to publish without asking.
.archive-documents/
